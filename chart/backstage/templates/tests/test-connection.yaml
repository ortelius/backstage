apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backstage.fullname" . }}-test-connection"
  labels:
    {{- include "backstage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['backstage.flux-system.svc.cluster.local:{{ .Values.service.port }}']
  restartPolicy: Never
